# THE GLASS COMPANY

**LIVE: https://sjgant80-hub.github.io/glass-company/**

A business you can audit while it runs. "Demonstrable human oversight of autonomous systems" as a
NUMBER with a heartbeat, not a policy PDF:

- `heartbeat.mjs` — THE PULSE LAW: from the operator's real records it derives the seam (does
  approval conduct to execution — the κ-band), the doors awaiting the human key (kind/stream only —
  **door contents cannot cross into the pulse, by construction, and that refusal is mutation-tested**),
  and fixed verdict sentences: the same state reads the same sentence for every auditor, forever.
  Witness 15/16 + 1 argued equivalent, CLEAN.
- `seam.mjs` — vendored from si-didy-loop (gated upstream): RISEN = executed/approved, healthy in
  the κ-band [0.618, 0.687].
- The constitution table on the page lists every governing law with its repo and the one command
  that re-proves it — mutants injected, tests must kill them.

Signed, expiring verdicts for the constitution live on [THE PROVEN WEB](https://sjgant80-hub.github.io/proven-web/).
The pulse regenerates nightly from real records; an invented pulse is refused at the generator.

Konomi Architecture — created by Thomas Frumkin · konomi-systems.com · built by AI-Native Solutions.
